#!/usr/bin/env node
/* eslint no-console: 0 */

(async () => {
  try {
    // TODO: create global command
    console.log('todo: create global command')
  } catch (error) {
    console.error(error)
    process.exit(1)
  }
})()
